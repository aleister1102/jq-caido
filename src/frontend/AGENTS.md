# Frontend (Caido plugin UI)

Scope: `src/frontend/**`. Run commands from **repo root** (see [root AGENTS.md](../../AGENTS.md)).

## Quick Reference

| Goal | Command |
|---|---|
| Build | `PATH="/opt/homebrew/bin:$PATH" /opt/homebrew/bin/node node_modules/.bin/caido-dev build` |
| Watch | `PATH="/opt/homebrew/bin:$PATH" /opt/homebrew/bin/node node_modules/.bin/caido-dev watch` |

## Detailed Instructions

- [Structure](.agents/structure.md) — source layout and entry points
- [Rules](.agents/rules.md) — performance, Caido integration, bundling
